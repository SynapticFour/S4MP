# s4-project

Project workspace and configuration contracts.

## Responsibility

Defines how S4MP projects are opened, configured, locked, and referenced by snapshot.

## Public API

| Module | Purpose |
|--------|---------|
| `workspace` | `Workspace` trait |
| `config` | `ProjectConfig` |
| `lockfile` | `Lockfile` |
| `snapshot` | `SnapshotRef` |

## Tier

1 — Infrastructure
