# s4-api

HTTP and gRPC API contracts for remote S4MP access.

## Public API

| Module | Purpose |
|--------|---------|
| `server` | `ApiServer` trait |
| `routes` | Route identifiers and request/response types |
| `transport` | `Transport`, `TransportConfig` |

## Tier

4 — Surfaces
