/// Minimal test harness placeholder for plugin integration tests.
pub struct PluginTestHarness;

impl PluginTestHarness {
    pub fn new() -> Self {
        Self
    }
}

impl Default for PluginTestHarness {
    fn default() -> Self {
        Self::new()
    }
}
